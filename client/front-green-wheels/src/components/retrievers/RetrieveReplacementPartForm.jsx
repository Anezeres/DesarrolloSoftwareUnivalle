import { ReplacementPartForm } from "../forms/ReplacementPartForm"

export const RetrieveReplacementPartForm = (selectedOption, createMode) => {
    return <ReplacementPartForm createdMode={createMode}/>    
}